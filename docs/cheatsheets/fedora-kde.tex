% Fedora KDE printable cheat sheet.
% Source of truth for the layout shortcut: docs/platforms/fedora.md, "Keyboard layouts"
% (Plasma 6's own Meta+Alt+K default, confirmed enabled by hand -- this
% repository does not script KDE global shortcuts). Regenerate with
% docs/cheatsheets/generate.sh after editing the README section or this file.
\documentclass[10pt]{extarticle}
\usepackage{cheatsheet}

\begin{document}
\cssheettitle{Fedora KDE}{Normal KDE Plasma / Wayland workstation reference}

\begin{multicols}{2}

\cssection{Desktop \& keyboard layout}
\begin{cskeys}{0.30\linewidth}
\csrow{Meta+Alt+K}{Switch US/Danish keyboard layout}
\end{cskeys}
\csnote{This is Plasma 6's own default \emph{Switch to Next Keyboard Layout} shortcut, not a dotfiles binding --- it appears once both layouts are added under System Settings $\to$ Keyboard $\to$ Layouts. The tray keyboard-layout indicator shows the active layout.}
\csnote{Everything else about the KDE desktop --- launcher, window shortcuts, screenshots, clipboard history --- is Plasma's stock configuration, not tracked here. See System Settings $\to$ Shortcuts.}

\cssection{Theme}
% csprose: theme.preserve-wallpaper
\csnote{The shared \texttt{theme <f>} command below also changes the Plasma global theme, colour scheme and cursor. \texttt{theme <f> --preserve-wallpaper} keeps the current desktop background; the lock screen always follows the flavour.}

\renewcommand{\cstermlegend}{\cslegend{\texttt{ghostty +list-keybinds --default} $\to$ exact tab/split/zoom/reload keys. Copy/paste: \texttt{Ctrl+Shift+C/V}.}}
\input{common-workflow}

\end{multicols}
\csfoot{Full keybindings reference: docs/reference/keybindings.md. Layout-switch source: docs/platforms/fedora.md. Regenerate: docs/cheatsheets/generate.sh.}
\end{document}
